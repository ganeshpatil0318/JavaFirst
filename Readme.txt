1. Create calculator project in Eclips:

2. Upload that project on GitHub :
post code from system to remote repository
 git init
 git status
 git add .
 git status
 git remote add origin https://github.com/ganeshpatil0318/JavaFirst.git
 git commit -m "First" 	// -m = message
 git push -u origin master	// -u = UpStream

3. Fork(copy) Others project to owr repo in GitHub:
  click on fork button on that repo and fork to your GitHu repo.

4. Download project from remote repo to local system:

create empty folder and open GitBash in that folder

git clone URL
eg - git clone https://github.com/gsatkani/dayone-demo-77.git


write java program :

to find add or even
check number is positive / negative / zero
Greatest among two number / three number
Leap year or not
check character is lower case or upper case